module.exports = {
  project: {
    ios: {},
    android: {},
  },
  // Fonts are handled by fonts.gradle for Android and Info.plist for iOS
  // No need to use assets here to avoid duplicate resources
};

